{% set path = "assets/" + page.title + "-block.png" %}
{% if block_image_exists(path) %}
![{{page.title}}]({{path}}){{ block_image_sizing() }}
{% endif %}

## Overview

## Description

Processes chat messages using an LLM model with function calling capabilities, featuring two steps: chat for message processing and handle_tool_result for managing tool execution results.

Notes:
- This version operates within dataset scope constraints and shares the same conversation/tool-call semantics as 1.0.0 but with updated scoping.
- Steps:
  - `chat(message)`: process input and optionally request tool invocations.
  - `handle_tool_result(tool_call_id, tool_result)`: resume after tool execution.

{{ generate_block_details_smartspace(page.title) }}



## Example(s)
### Example 1: Chat within dataset scope
- Call `chat(message={...})` to generate a response using dataset-scoped context.
- If a tool is requested, run it and provide the result to `handle_tool_result`.

## Error Handling
 - Propagates model/tool errors; ensure `tool_result` matches the expected schema for the invoked tool.

## FAQ
???+ question "How is this different from 1.0.0?"

    Functionally similar, but scoped differently (dataset scope) and may have environment-specific integrations.
